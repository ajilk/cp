/* <title>
 *
 * <link>
 *
 * <notes>
 *
 */
static const auto speedup = []() {
    std::ios::sync_with_stdio(false); 
    std::cin.tie(nullptr); 
    return 0;
}();

#define fr(i, n) for (int i = 0; i < (n); ++i)

#include <iostream>
using namespace std;

int main() {
  /* code */
  return 0;
}
